# Development Environment Variables
# NEVER commit this file to version control with real credentials

# Database
DB_PASSWORD=development_password_123
DB_HOST=localhost
DB_PORT=6432  # PgBouncer port
DB_NAME=portfolio_db
DB_USER=portfolio_user

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Security (CHANGE THESE IN PRODUCTION)
SECRET_KEY=development_secret_key_change_in_production_abc123xyz789
JWT_SECRET_KEY=development_jwt_secret_change_in_production_def456uvw012
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24
REFRESH_TOKEN_EXPIRATION_DAYS=30

# API Configuration
API_V1_PREFIX=/api/v1
CORS_ORIGINS=["http://localhost:3000", "http://localhost:5173"]
RATE_LIMIT_PER_MINUTE=100

# yfinance Configuration
YFINANCE_CACHE_TTL_SECONDS=60  # Cache for 1 minute during development
YFINANCE_MAX_RETRIES=3
YFINANCE_RETRY_DELAY_SECONDS=1

# Market Data Providers (for future use)
ALPHA_VANTAGE_API_KEY=
IEX_CLOUD_API_KEY=
POLYGON_API_KEY=

# Background Jobs
JOB_QUEUE_HIGH_PRIORITY=high
JOB_QUEUE_DEFAULT_PRIORITY=default
JOB_QUEUE_LOW_PRIORITY=low
JOB_MAX_RETRIES=3
JOB_RETRY_DELAY_SECONDS=60

# Logging
LOG_LEVEL=DEBUG
LOG_FORMAT=json

# Development Settings
DEBUG=true
RELOAD=true
TESTING=false

# Frontend URLs
FRONTEND_URL=http://localhost:3000
WEBSOCKET_URL=ws://localhost:8000

# Email (for future use)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@finplanner.local

# Monitoring (for future use)
SENTRY_DSN=
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001