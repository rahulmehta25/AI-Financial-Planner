# API Configuration
VITE_API_URL=
VITE_WS_URL=ws://localhost:8000
VITE_ENABLE_MOCK_DATA=true
VITE_USE_SERVERLESS=true

# Production settings (when using Vercel)
# VITE_API_URL will be automatically set by Vercel to your deployment URL
# VITE_USE_SERVERLESS=true (default)
# VITE_ENABLE_MOCK_DATA=false (for production with real backend)

# Development settings
# VITE_API_URL=http://localhost:8000 (if using separate backend)
# VITE_USE_SERVERLESS=false (to use separate backend instead of serverless functions)